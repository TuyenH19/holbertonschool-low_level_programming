#include "main.h"

/**
 * leet - encode a string to 1337
 * @s: string input
 *
 * Return: current string which is encoded
 */

char *leet(char *s)
{
	int i = 0;
	char [5,3] = {(a,A,4), }

	while (s[i] != '\0')
	{
		if (s[i] == 'a' || s[i] == 'A')
			s[i] = '4';
	}
	return (s);
}
